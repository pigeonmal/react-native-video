///
/// HybridVideoDownloadManagerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `VideoDownloadTask` to properly resolve imports.
namespace margelo::nitro::video { struct VideoDownloadTask; }
// Forward declaration of `NativeVideoConfig` to properly resolve imports.
namespace margelo::nitro::video { struct NativeVideoConfig; }
// Forward declaration of `VideoDownloadOptions` to properly resolve imports.
namespace margelo::nitro::video { struct VideoDownloadOptions; }

#include "VideoDownloadTask.hpp"
#include <NitroModules/Promise.hpp>
#include "NativeVideoConfig.hpp"
#include "VideoDownloadOptions.hpp"
#include <string>
#include <vector>

namespace margelo::nitro::video {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `VideoDownloadManager`
   * Inherit this class to create instances of `HybridVideoDownloadManagerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridVideoDownloadManager: public HybridVideoDownloadManagerSpec {
   * public:
   *   HybridVideoDownloadManager(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridVideoDownloadManagerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridVideoDownloadManagerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridVideoDownloadManagerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<VideoDownloadTask>> enqueueDownload(const NativeVideoConfig& config, const VideoDownloadOptions& options) = 0;
      virtual std::shared_ptr<Promise<void>> pauseDownload(const std::string& downloadId) = 0;
      virtual std::shared_ptr<Promise<void>> resumeDownload(const std::string& downloadId) = 0;
      virtual std::shared_ptr<Promise<void>> removeDownload(const std::string& downloadId) = 0;
      virtual std::shared_ptr<Promise<VideoDownloadTask>> getDownload(const std::string& downloadId) = 0;
      virtual std::shared_ptr<Promise<std::vector<VideoDownloadTask>>> getDownloads() = 0;
      virtual std::shared_ptr<Promise<NativeVideoConfig>> resolveSourceForOffline(const NativeVideoConfig& config, const std::string& downloadId) = 0;
      virtual std::shared_ptr<Promise<void>> clearDownloads() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "VideoDownloadManager";
  };

} // namespace margelo::nitro::video
